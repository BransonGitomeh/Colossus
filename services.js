/**
 * this are all the ports the services are running in, and the roles they have pinned on them
 */

{
	_blogs:{
		host:"localhost"
		pinned:"blogs",
		port:4001
	},
	_users:{
		host:"localhost"
		pinned:"users",
		port:4000
	}
}